package com.didiglobal.knowframework.elasticsearch.client.parser.constant;

public enum DslItemType {
    SELECT,
    WHERE,
    WHERE_TERM,
    WHERE_RANGE,
    GROUP_BY,
    ORDER_BY,
    HAVING,
    NONE
}
