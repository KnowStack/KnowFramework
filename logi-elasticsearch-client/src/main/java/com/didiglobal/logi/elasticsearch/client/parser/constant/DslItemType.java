package com.didiglobal.logi.elasticsearch.client.parser.constant;

/**
 * @author D10865
 */
public enum DslItemType {
    SELECT,
    WHERE,
    WHERE_TERM,
    WHERE_RANGE,
    GROUP_BY,
    ORDER_BY,
    HAVING,
    NONE
}
